// normal function
function addNormalFunction(a: number, b: number): string {
  return `sum : ${a + b}`;
}
// console.log(addNormalFunction(2, 4));

// arrow function
const addArrowFunction = (a: number, b: number): number => a + b;
// console.log(addArrowFunction(2, 4));
